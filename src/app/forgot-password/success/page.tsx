import { Suspense } from "react";
import SuccessPage from "../_components/SuccessPage";

const Success = () => {
  return (
    <Suspense>
      <SuccessPage />
    </Suspense>
  );
};

export default Success;
